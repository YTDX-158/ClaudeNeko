@echo off
title ClaudeNeko
rem Launch ClaudeNeko silently (no black window), then open browser.
rem Portable: uses this file's own folder, works anywhere you unzip.
wscript //nologo "%~dp0launcher.vbs"
exit /b 0
